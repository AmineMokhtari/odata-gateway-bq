import { http, HttpResponse } from 'msw'

export const handlers = [
  // Add your mock handlers here
  // Example:
  // http.get('/v1/projects', () => {
  //   return HttpResponse.json([{ id: 'mock-project', name: 'Mock Project' }])
  // }),
]
